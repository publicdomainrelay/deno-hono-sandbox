{
  "lexicon": 1,
  "id": "com.publicdomainrelay.temp.compute.deno.workerConfig",
  "defs": {
    "main": {
      "type": "record",
      "description": "Configuration for a Deno sandbox worker.",
      "key": "tid",
      "record": {
        "type": "object",
        "required": ["payload"],
        "properties": {
          "payload": {
            "type": "string",
            "description": "JSON-encoded configuration payload for the worker."
          }
        }
      }
    }
  }
}
