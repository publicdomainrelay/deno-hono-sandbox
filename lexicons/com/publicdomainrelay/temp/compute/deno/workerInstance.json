{
  "lexicon": 1,
  "id": "com.publicdomainrelay.temp.compute.deno.workerInstance",
  "defs": {
    "main": {
      "type": "record",
      "description": "A signed record tracking a running Deno sandbox worker instance. References the workerManifest used to spawn it.",
      "key": "tid",
      "record": {
        "type": "object",
        "required": ["manifest", "signatures"],
        "properties": {
          "manifest": {
            "type": "ref",
            "ref": "com.atproto.repo.strongRef",
            "description": "Strong reference to the com.publicdomainrelay.temp.compute.deno.workerManifest record this instance was created from."
          },
          "signatures": {
            "type": "ref",
            "ref": "network.attested.signature#signatures",
            "description": "badge.blue attestations over this instance record. Must include the service's inline signature."
          }
        }
      }
    }
  }
}
