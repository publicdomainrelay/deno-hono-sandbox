{
  "lexicon": 1,
  "id": "com.publicdomainrelay.temp.compute.deno.workerRequest",
  "defs": {
    "main": {
      "type": "object",
      "description": "An HTTP request forwarded to a running Deno sandbox worker. Used as input for executeWorkerInstance and other worker-facing procedures.",
      "required": ["method", "path"],
      "properties": {
        "method": {
          "type": "string",
          "description": "HTTP method (GET, POST, PUT, DELETE, etc.)."
        },
        "path": {
          "type": "string",
          "description": "Request path (e.g. /api/echo)."
        },
        "headers": {
          "type": "object",
          "description": "Request headers. Keys are header names, values are header values.",
          "additionalProperties": { "type": "string" }
        },
        "body": {
          "type": "unknown",
          "description": "Optional request body."
        }
      }
    }
  }
}
